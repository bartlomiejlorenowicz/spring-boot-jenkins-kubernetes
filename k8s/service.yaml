apiVersion: v1
kind: Service
metadata:
  name: deploy-lab
  namespace: deploy-lab
spec:
  selector:
    app: deploy-lab
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
